{
  "buildCommand": "cp -r ../backend . && npm run build",
  "functions": {
    "api/backend.py": {
      "maxDuration": 60
    }
  },
  "rewrites": [
    { "source": "/api/v1/:path*", "destination": "/api/backend" }
  ]
}
